#pragma once

namespace plumeria {
namespace config {

// This firmware intentionally runs in standalone mode.
inline constexpr bool kCompanionEnabled = false;

}  // namespace config
}  // namespace plumeria
